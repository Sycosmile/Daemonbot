"""handlers/leaderboard.py — placeholder for reaction-based call tracking (future)"""

from telegram import Update
from telegram.ext import ContextTypes


async def handle_call_reaction(update: Update, ctx: ContextTypes.DEFAULT_TYPE):
    """Future: track emoji reactions to calls for scoring."""
    pass
